version: '3.8'

services:
  # Database service will be configured here
  # Example:
  # database:
  #   image: postgres:latest
  #   environment:
  #     POSTGRES_DB: ai_news
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:

